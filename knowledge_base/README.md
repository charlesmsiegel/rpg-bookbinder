# Knowledge Base

Optional wiki of extracted setting knowledge, maintained by the knowledge-base
agents and searched via the kb MCP server. Set `knowledge_base.top_level_dirs`
in `config/system.json` to scaffold your taxonomy (e.g. factions, places,
mechanics, history, characters). Files are markdown with relative `[[links]]`.
If your KB derives from licensed books, consider gitignoring it.
