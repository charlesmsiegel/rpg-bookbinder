# Book Templates

Structural blueprints for kinds of books (e.g. "faction book", "location book",
"genre anthology"): chapter lists, expected word counts per section, required
elements. `/plan-project` consults these when outlining. One markdown file per
template.
